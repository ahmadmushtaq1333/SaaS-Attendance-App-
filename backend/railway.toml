[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python manage.py migrate --noinput && gunicorn --bind 0.0.0.0:$PORT --workers 2 --timeout 120 attendance_saas.wsgi:application"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
